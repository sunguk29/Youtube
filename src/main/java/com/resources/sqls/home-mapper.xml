<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mapper.HomeMapper">
        <insert id="videoInsert" useGeneratedKeys="true" keyProperty="no"  parameterType="Video">
            insert into video
            (no, type, user_no, thumbnail, views, reg_datetime, update_datetime,
             likes, dislikes, title, description, time, video_file)
            values(#{no},#{type},#{userNo},#{thumbnail},#{views},#{regDatetime},#{updateDatetime},#{likes},#{dislikes},#{title},#{description},#{time},#{videoFile})
        </insert>



</mapper>